import styles from "./MainMeterType.module.scss";
import { PropsMainMeterType } from "./interfaces";

function MainMeterType({}: PropsMainMeterType) {
  return <div>MainMeterType</div>;
}

export default MainMeterType;
